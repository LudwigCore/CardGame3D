using UnityEngine;
public class CardData : MonoBehaviour
{
    public string Rune;// The name of the card
    public string Type;// what element it is
    public int Level;// The tier the card is also shows to the enemy on the back
    public string Ability;// What ability the card uses
    public int AbilityValue;// The number that the ability uses
    public int Turns;// How long a card lasts after placing
}